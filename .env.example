# Base URL of the FleetPilot backend API (no trailing slash).
# Local dev:   http://localhost:4000
# Production:  https://your-service.onrender.com
# Leave unset to run the app in demo mode with local sample data.
VITE_API_URL=
